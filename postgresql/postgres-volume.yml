apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-volume
  labels:
    name: postgres-volume
spec:
  capacity:
    storage: 50Gi
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  gcePersistentDisk:
    pdName: postgresql-disk
    fsType: ext4
